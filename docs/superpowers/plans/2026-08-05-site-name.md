# Site Name Update Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将所有面向用户的网站名称统一为“周三充值日拼图互助”，同时保留 `cmcc-puzzle-hub` 技术标识。

**Architecture:** 直接更新 Next.js 静态元数据和首页一级标题，不新增运行时状态或配置层。通过页面渲染测试和元数据导出测试锁定名称，README 与部署指南同步更新。

**Tech Stack:** Next.js 16、React 19、TypeScript、Vitest、Testing Library、pnpm

---

### Task 1: 用失败测试定义网站名称

**Files:**
- Create: `src/app/page.test.tsx`
- Create: `src/app/layout.test.ts`

- [ ] **Step 1: 编写首页名称测试**

```tsx
import { cleanup, render, screen } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";

vi.mock("@/features/posts/components/post-feed", () => ({
  PostFeed: () => <div />,
}));
vi.mock("@/features/posts/components/post-filters", () => ({
  PostFilters: () => <div />,
}));

import Home from "./page";

describe("Home", () => {
  afterEach(cleanup);

  it("显示网站名称", async () => {
    const page = await Home({
      params: Promise.resolve({}),
      searchParams: Promise.resolve({}),
    });
    render(page);

    expect(
      screen.getByRole("heading", { level: 1, name: "周三充值日拼图互助" }),
    ).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 编写元数据名称测试**

```ts
import { describe, expect, it, vi } from "vitest";

vi.mock("next/font/google", () => ({
  Geist: () => ({ variable: "--font-geist-sans" }),
  Geist_Mono: () => ({ variable: "--font-geist-mono" }),
}));

import { metadata } from "./layout";

describe("site metadata", () => {
  it("使用完整网站名称", () => {
    expect(metadata.title).toBe("周三充值日拼图互助");
    expect(metadata.description).toBe("周三充值日拼图互助大厅");
  });
});
```

- [ ] **Step 3: 运行测试并确认因旧名称失败**

Run: `pnpm exec vitest run src/app/page.test.tsx src/app/layout.test.ts`

Expected: 两个名称断言失败，实际值分别包含“拼图大厅”“拼图互助”和“中国移动拼图互助大厅”。

### Task 2: 更新网站名称和项目说明

**Files:**
- Modify: `src/app/layout.tsx`
- Modify: `src/app/page.tsx`
- Modify: `README.md`
- Modify: `docs/零预算部署与开源指南.md`

- [ ] **Step 1: 更新 Next.js 元数据**

```ts
export const metadata: Metadata = {
  title: "周三充值日拼图互助",
  description: "周三充值日拼图互助大厅",
};
```

- [ ] **Step 2: 更新首页主标题**

```tsx
<h1 className="text-2xl font-bold tracking-tight text-slate-950">
  周三充值日拼图互助
</h1>
```

- [ ] **Step 3: 更新文档名称**

将 `README.md` 标题改为 `# 周三充值日拼图互助`，简介改为“移动端优先的周三充值日拼图互助 H5”。将部署指南开头的“中国移动拼图互助平台”改为“周三充值日拼图互助”。保留免责声明中的“中国移动”商标说明和所有 `cmcc-puzzle-hub` 技术标识。

- [ ] **Step 4: 运行名称测试并确认通过**

Run: `pnpm exec vitest run src/app/page.test.tsx src/app/layout.test.ts`

Expected: 2 个测试文件全部通过。

### Task 3: 全量验证、提交和推送

**Files:**
- Verify: `src/app/layout.tsx`
- Verify: `src/app/page.tsx`
- Verify: `src/app/layout.test.ts`
- Verify: `src/app/page.test.tsx`
- Verify: `README.md`
- Verify: `docs/零预算部署与开源指南.md`

- [ ] **Step 1: 检查名称和格式**

Run: `rg -n '中国移动拼图互助平台|中国移动拼图互助 H5|title: "拼图互助"|>拼图大厅<' src README.md docs/零预算部署与开源指南.md`

Expected: 不再出现作为网站名称的旧文本；免责声明和历史设计文档不在本次扫描范围。

Run: `git diff --check`

Expected: 无输出，退出码为 0。

- [ ] **Step 2: 执行完整验证**

Run: `pnpm lint`

Expected: 退出码为 0。

Run: `pnpm typecheck`

Expected: 退出码为 0。

Run: `pnpm test:unit`

Expected: 所有单元测试通过。

Run: `pnpm build`

Expected: Next.js 生产构建成功。

- [ ] **Step 3: 提交实现**

```powershell
git add -- src/app/layout.tsx src/app/page.tsx src/app/layout.test.ts src/app/page.test.tsx README.md 'docs/零预算部署与开源指南.md' 'docs/superpowers/plans/2026-08-05-site-name.md'
git commit -m "feat: rename site to Wednesday puzzle hub"
```

- [ ] **Step 4: 推送并验证远端**

Run: `git push origin main`

Expected: `main` 推送成功，本地 `HEAD` 与远端 `main` SHA 一致。
